import { firebaseConfig } from './config/firebase';
import { ngForageConfig } from './config/ngforage';

export const environment = {
  production: true,
  firebase: firebaseConfig,
  ngforage: ngForageConfig,
};
